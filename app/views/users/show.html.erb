<h1><%= @user.email %>'s Created Events</h1>

<div id="events">
  <% @user.created_events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
</div>